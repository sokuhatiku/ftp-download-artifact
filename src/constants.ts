export enum Inputs {
  Name = 'name',
  Path = 'path',
  Host = 'host',
  Port = 'port',
  Username = 'username',
  Password = 'password',
  Secure = 'secure',
  RemotePath = 'remote-path'
}
export enum Outputs {
  DownloadPath = 'download-path'
}
