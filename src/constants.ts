export enum Inputs {
  Name = 'name',
  Path = 'path',
  Server = 'server',
  Port = 'port',
  Username = 'username',
  Password = 'password',
  RemotePath = 'remote-path'
}
export enum Outputs {
  DownloadPath = 'download-path'
}
