export enum Inputs {
  Name = 'name',
  Path = 'path',
  Host = 'host',
  Port = 'port',
  Username = 'username',
  Password = 'password',
  RemotePath = 'remote-path'
}
export enum Outputs {
  DownloadPath = 'download-path'
}
